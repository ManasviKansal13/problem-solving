#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    //no of inputs;
    int count,A[0];
    printf("Check");
    scanf("%d",&count);
    printf("Check");
    for(int i=0;i<=count;i++){
        int j;
        scanf("%d\t",&A[j]);
    }  
    printf("Check");
    int sum=0;
    for(int i=0;i<=count;i++){
        sum=sum+i;
    } 
    printf("Check");
    printf("%d",sum);
    return 0;
}